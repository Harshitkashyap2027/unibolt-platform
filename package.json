{
  "name": "unibolt-platform",
  "version": "1.0.0",
  "description": "UniBolt — Enterprise-grade gamified internship and recruitment platform",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
